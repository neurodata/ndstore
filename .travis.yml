language: python

python:
  - 2.7

notifications:
  email: false

sudo: required

dist: trusty

#services:
  #- mysql
  #- memcached
  #- rabbitmq
  #- supervisor
  #- nginx

before_install:
  #- sudo apt-get update -qq
  - sudo ./setup/neurodata_install.sh

install: 
  - sudo apt-get install libhdf5-serial-dev
  - pip install cython numpy
  - pip install -r ./setup/requirements.txt

cache:
  - pip
  - apt

script:
  - cd /home/neurodata/test/
  - py.test

; ==============================================
;  celery worker -- prefetch processes
; ==============================================

[program:celery]
command=/home/klillan1/open-connectome/django/manage.py celery worker --loglevel=INFO -Q celery
directory=/home/klillan1/open-connectome/ocpca
user=klillan1
numprocs=1
stdout_logfile=/var/log/celery/async.log
stderr_logfile=/var/log/celery/async.log
autostart=true
autorestart=true
startsecs=10
process_name=ocp_async_%(process_num)s

; Need to wait for currently executing tasks to finish at shutdown.
; Increase this if you have very long running tasks.
stopwaitsecs = 600

; if rabbitmq is supervised, set its priority higher
; so it starts first
priority=998
